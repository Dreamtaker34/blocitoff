<div id=<%="item-#{item.id}"%>>
  <%= link_to "", user_item_path(user, item), method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
  <%= item.name %>
  <small><p>You have <%= distance_of_time_in_words(item.created_at, Time.now - 7.days, include_seconds: true) %> left to complete this.</p></small>
</div>
