<div id=<%="item-#{item.id}"%>>
  <%= link_to "", user_item_path(user, item), method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
  <%= item.name %>
  <font style= "color: #888888"><small><p><%= item.time_left %> left to complete this.</p></small></font>
</div>
